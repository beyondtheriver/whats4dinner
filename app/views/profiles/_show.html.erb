
  <h2><%= profile.f_name %>'s Profile</h2>
  <p>Name: <%= profile.f_name %> <%= profile.l_name %></p>

   <%= button_to 'Edit Profile', edit_profile_path(current_user), method: :get, :class => "form_button" %><br>
  <h2><%= profile.f_name %>'s Recipes</h2>
<%# Insert links to recipes here -%>

<p>Unhappy? <%= button_to "Cancel my account", "/users/#{current_user.id}/delete", method: :delete, :class => "form_button" %></p>
