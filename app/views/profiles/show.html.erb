<div id="banner">
  <h2><%= link_to "What's 4 Dinner", recipes_index_path %></h2>
</div>
<div id="profile_wrapper">
  <h2><%= @profile.f_name %>'s Profile</h2>
  <p>Name: <%= @profile.f_name %> <%= @profile.l_name %></p>
  <%= button_to 'Edit Profile', edit_profile_path(current_user), :class => "form_button", method: :get %><br>
  <%= button_to "Cancel My Account", "/users/#{current_user.id}/delete", method: :delete %>

  <h2><%= @profile.f_name %>'s Recipes</h2>
<%# Insert links to recipes here -%>
</div>
