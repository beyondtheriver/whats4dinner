
  <h2><%= @profile.f_name %>'s Profile</h2>
  <p>Name: <%= @profile.f_name %> <%= @profile.l_name %></p>

  <h2><%= @profile.f_name %>'s Recipes</h2>
<%# Insert links to recipes here -%>

<%= link_to 'Edit Profile', edit_profile_path(current_user), method: :get %><br>

<%= link_to "Delete My Account", cancel_registration_path(current_user), :data => { :confirm => "Are you sure?" }, :method => :delete %>